# 开发环境的环境变量（命名必须以 VITE_ 开头）

## CacheCloud 后端地址（Vite 开发代理 target）
VITE_CACHECLOUD_BACKEND = http://127.0.0.1:8080

## 前端请求前缀（对接 Spring Boot /api/v1，迁移期由 vite proxy 转发）
VITE_BASE_URL = /api/v1

## 开发环境域名和静态资源公共路径（一般 / 或 ./ 都可以）
VITE_PUBLIC_PATH = /
