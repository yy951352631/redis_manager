<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shcj.cache.dao.BenchmarkDao">

    <sql id="cols">
        app_id,app_name,target_desc,options_json,status,total_requests,error_count,qps,
        p50_ms,p95_ms,p99_ms,max_ms,avg_ms,client_cpu_percent,
        command_stats_json,error_stats_json,user_name,error_msg,start_time,end_time
    </sql>

    <insert id="save" parameterType="com.shcj.cache.benchmark.BenchmarkTask"
            useGeneratedKeys="true" keyProperty="id">
        insert into benchmark_task(<include refid="cols"/>)
        values(#{appId},#{appName},#{targetDesc},#{optionsJson},#{status},#{totalRequests},#{errorCount},#{qps},
               #{p50Ms},#{p95Ms},#{p99Ms},#{maxMs},#{avgMs},#{clientCpuPercent},
               #{commandStatsJson},#{errorStatsJson},#{userName},#{errorMsg},#{startTime},#{endTime})
    </insert>

    <update id="update" parameterType="com.shcj.cache.benchmark.BenchmarkTask">
        update benchmark_task set
            status = #{status}, total_requests = #{totalRequests}, error_count = #{errorCount}, qps = #{qps},
            p50_ms = #{p50Ms}, p95_ms = #{p95Ms}, p99_ms = #{p99Ms}, max_ms = #{maxMs}, avg_ms = #{avgMs},
            client_cpu_percent = #{clientCpuPercent},
            command_stats_json = #{commandStatsJson}, error_stats_json = #{errorStatsJson},
            error_msg = #{errorMsg}, end_time = #{endTime}
        where id = #{id}
    </update>

    <select id="get" resultType="com.shcj.cache.benchmark.BenchmarkTask">
        select id,<include refid="cols"/> from benchmark_task where id = #{id}
    </select>

    <select id="search" resultType="com.shcj.cache.benchmark.BenchmarkTask">
        select id,<include refid="cols"/>
        from benchmark_task
        <where>
            <if test="appId != null">and app_id = #{appId}</if>
        </where>
        order by id desc
        limit #{offset}, #{limit}
    </select>

    <select id="count" resultType="int">
        select count(1) from benchmark_task
        <where>
            <if test="appId != null">and app_id = #{appId}</if>
        </where>
    </select>

    <delete id="deleteBefore">
        delete from benchmark_task where start_time &lt; #{before} limit #{batchSize}
    </delete>
</mapper>
